# Local scripts

`touhou.txt` at the repo root is **private** (gitignored). Edit it freely;
it will never be pushed.

Content currently on the site was synced from that file by hand / one-off
scripts. To re-sync later, re-run the content update workflow in this repo
or ask the agent to re-apply `touhou.txt` without committing the file.
