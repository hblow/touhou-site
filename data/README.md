# Reserved: data/

This directory is reserved for a future YAML-based content scale-out.

v1 content lives as Markdown under `content/characters`, `content/media`, and
`content/fangames`. Do not add a data loader until item count or filtering
pain justifies a plugin under `plugins/`.
